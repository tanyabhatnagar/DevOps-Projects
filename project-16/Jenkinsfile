pipeline {
    agent {
        docker {
            image 'tanya119/maven-docker:latest'
            args '-v /var/run/docker.sock:/var/run/docker.sock -v maven-repo:/home/jenkins/.m2 --user 1000'
        }
    }
    
    environment {
        DOCKER_IMAGE = "tanya119/project16-app:latest"
        DOCKER_HUB_CREDENTIALS = credentials('51611545-1f09-453a-a2a6-e0120a9347f2')
    }
    
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        
        stage('Build JAR') {
            steps {
                dir('project-16') {
                    sh 'mvn clean package -DskipTests'
                    // Verify exact JAR filename
                    sh 'JAR_FILE=$(ls target/project-16-*.jar) && echo "JAR exists at: ${JAR_FILE}"'
                }
            }
        }
        
        stage('Build Docker Image') {
            steps {
                dir('project-16') {
                    // Force-copy the JAR to a predictable name first
                    sh '''
                    JAR_FILE=$(ls target/project-16-*.jar)
                    cp ${JAR_FILE} target/application.jar
                    docker build -t ${DOCKER_IMAGE} .
                    '''
                }
            }
        }
        
        stage('Push Docker Image') {
            steps {
                withCredentials([usernamePassword(
                    credentialsId: '51611545-1f09-453a-a2a6-e0120a9347f2',
                    usernameVariable: 'DOCKER_USER',
                    passwordVariable: 'DOCKER_PASS'
                )]) {
                    sh '''
                    echo "$DOCKER_PASS" | docker login -u "$DOCKER_USER" --password-stdin
                    docker push ${DOCKER_IMAGE}
                    docker logout
                    '''
                }
            }
        }
    }
    
    post {
        always {
            cleanWs()
        }
    }
}
